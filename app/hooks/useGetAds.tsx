// call flask api


import { database, Query } from "@/libs/AppWriteClient"
import { Ad } from "../types";

const useGetAds = async (user_id: String) => {
    try {
        
    } catch (error) {
        throw error
    }
}

export default useGetAllPosts